# 变更日志

本文件记录本项目所有重要变更。使用说明见 [README.zh-CN.md](README.zh-CN.md)。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/);版本遵循 [SemVer](https://semver.org/lang/zh-CN/)。

## [未发布]
- Trunk 分支模型 + GitHub Actions CI(测试门禁 + 打标签自动发布)。

## [1.14.0] - 2026-09-20
### 变更
- 界面文案「专注」统一改为「累计」(报表、每日详情、通知渠道名等),中英同步。
- 热力图色阶由**四色渐进改为五色渐进**,并把分档从纯绝对标准改为**绝对 + 相对结合**:
  四个边界取非零日总量的 **20/40/60/80 分位**(相对,随个人量级伸缩),每个分位再被夹在绝对区间内
  (第一档边界 20~40 分钟、第二档 40 分钟~1.5 小时、第三档 1~3 小时、第四档 1.5~6 小时),
  保证数据极少/极多时色阶仍有实际意义;有效数据不足 3 天时退回缺省绝对边界(30 分钟 / 1 小时 / 2 小时 / 4 小时)。

### 新增
- **疲劳提醒**:同一任务连续工作(忽略中间短休息)累计到 **90 分钟**时提醒长休息 15-20 分钟。
  依据超日节律(ultradian rhythm / BRAC,注意力约 90 分钟一个周期)与持续注意研究(60-90 分钟后
  警觉性下降)。口径:同一配置的工作段累计,段间空档 ≤30 分钟视为短休息忽略、>30 分钟视为真正
  休息(连续计数清零);连续工作仍在继续时至少间隔 30 分钟才再提醒一次;设置页可开关(默认开)。
  提醒形式:一条 30 秒后自动消失的独立通知 + 按系统模式振动(静音不振动;不响铃)。

## [1.13.0] - 2026-09-20
### 变更
- **提醒随系统静音/振动/响铃模式自动适配**(不再只看应用内强度):
  - 静音:仅发一条通知,6 秒后**自动消失**(不振动、不响铃);
  - 振动:仅振动(不发通知);
  - 响铃:振动 + 系统闹铃音(不发通知,铃声按时长自停)。
- **提醒强度**语义收窄为"振动节奏 + 铃声时长":轻 = 单次短振/2s,标准 = 两次短振/3s,
  强 = 三次长振/5s。轻档不再是"什么都不做"(旧实现下振动模式下选轻等于没有提醒)。

### 修复
- **备份目录不再每次重启后失效**:选择目录时立即 `takePersistableUriPermission` 持久化 SAF 授权
  (此前从未调用 —— 授权只活到进程结束,真机 `dumpsys activity` 里查不到本应用任何持久化授权),
  并在应用启动时自愈补做;写入失败区分"授权失效"(提示重选目录)与一般写入失败(提示目录可写性)。

## [1.12.3] - 2026-09-20
### 变更
- **阶段提醒改为只振动、不响铃**:不再播放系统闹铃/通知音,图书馆、课堂、会议场合不再外放。
  强度设置仍决定振动方式(LIGHT = 完全安静,STANDARD = 两次短振,STRONG = 三次长振)。

### 新增(仅 debug 构建)
- **诊断日志落盘**:每条同时写 logcat(`EmberDiag`)与
  `Android/data/com.embertimer/files/diag.log`(超 512 KiB 滚动为 `diag.log.1`),
  屏幕关闭期间发生的问题不再随进程消失,`adb pull` 即可取。
- **负计时证据日志**:到期推进记录迟到量(`到期推进 迟到=<ms>`、`ticker 到期推进 迟到=<ms>`)
  并附带 Doze/屏幕状态(`idle=... screen=...`);闹钟武装/取消也记录是否精确与目标时刻。

### 修复
- **到期唤醒不再被 OEM 冻结推迟**:主闹钟改用 `setAlarmClock`(系统级「用户可见闹钟」,
  不受华为/荣耀后台冻结与省电策略推迟)。真机实测(荣耀 REA-AN00 / Android 15):原
  `setExactAndAllowWhileIdle` 主闹钟在锁屏后被推迟 **43.7 秒**才送达(系统日志显示 CPU 醒着、
  只有本应用被冻结),阶段推进跟着晚 —— 这 43 秒里通知栏系统 Chronometer 就在显示负数。
  代价:计时期间状态栏出现闹钟图标。安全网闹钟仍为 elapsed 轴精确闹钟,墙钟被改动时兜底。
- **进程存活期间通知倒计时不再显示负数**:通知发布后在阶段到期 +250ms 主动重发一次,
  使迟到的 ticker/闹钟落到「已过期 → 静态 00:00」分支,而不是让系统 Chronometer 继续往负数走。

## [1.12.2] - 2026-09-16
### 新增(仅 debug 构建)
- **应用内诊断面板**:设置页「诊断(debug)」实时显示
  ①通知栏状态(我们那条通知是否在栏上、ongoing / 前台服务标记 / 渠道 / 发布时间)
  ②后台运行态(应用内服务是否存活、App 在前台还是后台)
  ③关键事件滚动日志(进程启动、前后台切换、服务 onCreate/onDestroy、到期闹钟送达、到期推进、
  命令、事件、通知发布)—— 内存 80 条环形缓冲,不落盘、不上传。
  正式版此区块不渲染、日志零开销(`FLAG_DEBUGGABLE` 门控)。
- 构建参数 `-PreleaseSignDebug=true`:debug 变体使用**正式签名**,可直接覆盖已安装的正式版而**不丢数据**
  (此前 debug 签名与正式不一致,必须卸载重装)。

## [1.12.1] - 2026-09-15
### 修复(真机实测发现的数据问题)
- **终止/重启后时间段不入账**:专注窗口(段起点 + 暂停空档)此前保存在引擎**内存字段**里,
  进程被系统回收或闹钟唤醒新进程后就丢了 —— 于是"终止后该段没计入数据"。现改为**随快照持久化**
  (DataStore 往返),任何进程都能落出完整时间段。
- **合计与时间段对不上**:检查点增量与段落**双份累加**(真机实测:今日合计 91.2 分钟,各时间段
  之和只有 57.8 分钟)。现检查点只推进游标、不再单独累加合计,合计唯一来源 = 段落派生,
  "今日合计 == 各时间段之和"恒成立;升级时一次性重算全部日期自洽。
- 落段门控改看**窗口时长**(此前看结算增量:一次 60s 检查点后增量为 0,整段被跳过 —— 同一根因)。

## [1.12.0] - 2026-09-12
### 变更(计时模块重构)
- **引擎驱动下沉到进程级协调器** `EngineCoordinator`:事件订阅随 AppGraph 建立,因此**只有闹钟广播
  唤起的进程**(前台服务起不来时)也能完整推进阶段——结算落库、提醒、通知换阶段、下一段武装全部完成。
  此前驱动绑在前台服务上,闹钟响了但服务起不来时推进就丢了,表现为"通知倒计时进负秒但阶段不切换"。
- **到期闹钟改为双闹钟冗余**(不使用 setAlarmClock,保持状态栏图标不变):主闹钟在到期时刻、安全网闹钟
  在到期 +45s;两者都送达同一接收器,推进幂等(仅"运行中且已到期"才动作),主闹钟被 OEM/Doze 吞掉时
  仍有第二次机会。
- 服务瘦身为宿主(前台化 / ticker / 生命周期收尾);`DeathReconciler` 并入协调器;通知发布改为 Context 版
  (无服务时也能更新通知)。
- 已过期快照仍写静态 `00:00`(不再出现负数计时)。

## [1.11.3] - 2026-09-11
### 变更
- **时段规则落到数据层**(不再只是展示层过滤):一次专注结束时,暂停间隔 <=3 分钟的前后段先合并,
  合并后仍不足 3 分钟的段落**不写入数据库**(不存储、不显示、不计入合计);入库切分阈值与展示阈值统一为 3 分钟。
- 升级时一次性清洗历史行(删除过短段、合并可合并段)并重算各日合计(normalized_v1111 标记)。
### 修复
- **备份文件损坏**:覆盖写改为"先删除同名文件再新建"——此前直接 openOutputStream 覆盖,在外部存储
  provider 上不截断,新 JSON 比旧的短时旧内容尾部残留,导致备份文件无法解析(实测踩到)。

## [1.11.1] - 2026-09-11
### 修复
- **每日详情时段规则回归**:合并后仍不足 **3 分钟**的段落整段丢弃(不显示、不计入合计);
  间隔 ≤3 分钟合并;合计与展示**共用同一函数**,因而"总累计 == 可见时间段之和"恒成立。
- **负计时**:轮询节拍锚定到期时刻(不足 15s 时按剩余时间等待)→ 到点即有 tick,
  系统 Chronometer 不再越过 00:00 继续向下计数。

## [1.11.0] - 2026-09-11
### 优化(体积 / 启动 / 耗电 / 内存)
- **备份可观测性**:SAF 授权失效导致写入失败时不再静默 —— 设置页显示一行红色提示"备份目录授权已失效…请重新选择备份目录"
  (此前失败无任何反馈,表现为"自动备份不生效")。
- **体积**:重新启用 R8(minify + 资源收缩)+ 剔除无用元数据 + 仅保留 arm64/armv7 ABI。
  APK **8.0MB → 1.68MB(-79%)**(真机实测;通知布局与全部图标资源经 aapt2 校验仍在)。
- **启动**:WorkManager 改为按需初始化(移出 Application.onCreate 关键路径);通知渠道创建与报表闹钟
  武装移到后台线程。真机冷启动 **486ms → 345ms**。
- **耗电**:到期轮询自适应节拍 —— 剩余时间 >5s 时 15s 一次、临近到点 500ms(此前固定 1s 轮询),
  长时段会话的服务唤醒次数降低约 15 倍;精确到点仍由 EXACT 闹钟负责,通知倒计时由系统 Chronometer 绘制。
- 内存:dex 缩小后 Java 堆占用下降(真机 PSS 97MB → 93MB;其中 Graphics 53MB 为 Compose 固有)。

## [1.10.12] - 2026-09-11
### 变更
- 报表期次下拉与首页下拉菜单统一格式:改为**布局流内全宽面板**(展开时报表内容整体顺沿下移),
  surface 背景 + 分隔线 + 全宽行 + 当前期次对号,动画与首页一致;不再使用 Popup 浮层。

## [1.10.11] - 2026-09-11
### 修复
- 通知栏倒计时不再出现负数:到点(Doze/进程冻结导致推进滞后)时,通知改为显示静态 00:00,
  系统 Chronometer 不再继续往负数走。
- 每日详情两列时间段的间距由 4dp 加大到 10dp;列宽改为 `widthIn(min=实测宽度)`,文本永不截断/被遮。
### 新增
- 提醒通知(「休息一下」/「开始工作」)右侧新增**对号图标按钮**:点击 = 确认收到并清除该通知,不打开应用。

## [1.10.10] - 2026-09-11
### 修复
- 每日详情时间段被遮挡:列宽由写死的 100dp 改为**按当前字体大小实测**(TextMeasurer),
  大字号/系统字体放大时不再截断"HH:mm ~ HH:mm",大时段标识也不会被遮。两列间距 4dp。

## [1.10.9] - 2026-09-11
### 变更
- 每日详情排版:大时段标识与时间段的间距由 0 加大到 12dp;两列时间段改为固定列宽(100dp)+ 2dp 紧贴,
  不再各占半屏(旧布局短文本时右侧留下大片空白)。实测:标识→时间段 11.6dp,时间段→时间段 1.8dp。
- 设置页移除全部说明性文字提示(备份说明、自动备份说明、卸载提示),只保留分区标题与控件/状态。

## [1.10.8] - 2026-09-11
### 变更
- **合计与每日详情时间段同源**:不再单独累加——每段工作结束后,当日合计 = 该日各配置段落按展示规则
  (间隔 <= 3 分钟视为连续)合并后的时长;入库切分阈值与展示合并阈值统一为 3 分钟。因此
  "总累计 == 每日详情各时间段之和"必然成立(旧数据升级时一次性重算)。
- **删除配置级联清理**:删除时钟时一并删除其专注段落与每日合计,每日详情/报表不再出现"已删除"占位行。
- **自动备份触发条件**:由"工作段结束"改为"检测到任意数据变动"(尤其是累计变动)——数据表心跳 +
  20 秒静默后入队一次(WorkManager 同名任务自动合并,不堆积)。
- 热力图每日详情的"上午/下午"等大时段标识颜色改为与卡片内其它文字一致(不再用强调色)。
### 移除
- 设置页的通知图标缓存提示文案(连同其设备判定代码)。

## [1.10.7] - 2026-09-11
### 修复
- **彻底消除"两个图标并列"**:不再在通知内容里自绘应用图标(上一版为让图标即时更新而自绘,
  与系统头部图标重复)。通知头部的应用图标由系统绘制(Android 13+ 通知标准,所有应用都有,应用无法移除),
  因此"只保留一个"的唯一正确做法就是不自绘。
### 新增(荣耀/华为定向适配)
- 识别荣耀/华为(MagicOS/EMUI)设备,在设置页给出**可执行的刷新指引**:系统会缓存通知图标,
  应用更新后可能仍显示旧图标,重启手机或切换一次系统主题即可刷新。
- 依据:10 组真机实验(改小图标为透明/彩色位图/绿色三角/新资源名、改应用图标配色/换图标资源名、
  setColor、去掉自定义布局)对系统徽标**均无影响**,且徽标配色与旧版图标逐值相同 -> 系统缓存,应用无权清除。

## [1.10.6] - 2026-09-11
### 新增
- 通知栏图标改由**应用自己渲染**(运行时读取当前应用图标并圆形裁切),因此**应用更新后通知图标立即更新,不需要重启手机**
  —— 实测:系统(荣耀 MagicOS)在通知里画的是**它自己缓存的应用图标**,该缓存不随应用更新刷新
  (改颜色/改形状/换资源名/卸载重装均无效),只有重启才重建。现在不依赖它了。
- 新增单测:通知不得自带 largeIcon(避免与系统头部图标重复)。

## [1.10.5] - 2026-09-10
### 修复
- 通知栏出现"新旧两个应用图标并列":移除了 v1.10.3 在通知内容里额外注入的 app 图标位图
  (系统本身就会画应用图标,再注入一份就成了两个)。新增守卫单测,禁止通知自带 largeIcon,
  防止后续改动再次引入重复图标。

## [1.10.4] - 2026-09-10
### 修复
- 通知栏/状态栏图标"看不清、与应用图标不像":把图标里的**火焰放大到安全区 ~70%**(原先只占 50% 高度),
  因为系统在通知栏会把应用图标**单色化**(实测真机:品牌橙底 + 同色浅调火焰),
  火焰过小时小尺寸下会被压成一个橙点。放大后通知栏图标与桌面图标的火焰轮廓一致、清晰可辨。

## [1.10.3] - 2026-09-10
### 修复
- 通知栏图标与桌面/应用图标不一致:
  - 通知内新增**真实应用图标位图**(取自 PackageManager,与桌面同源),并作为通知大图标 —— 通知里就是应用图标本身;
  - 状态栏小图标补 **5 档密度 PNG**(纯白火焰剪影,填满 88% 画布),不再依赖矢量在个别 ROM 上的缩放;
  - 自适应图标补 **monochrome 层**(Android 13+ 主题图标/系统单色渲染),避免平台把图标单色化成"实心圆 + 图形抠空"的怪异效果。

## [1.10.2] - 2026-09-10
### 变更
- 发布流程改由 GitHub Actions 自动完成:打 `v*` 标签即自动构建并发布 `EmberTimer-<版本>.apk`。
  仓库已配置签名 secrets,CI 产物为**正式签名**(与本地发布同签名,可直接覆盖安装)。

## [1.10.1] - 2026-09-10
### 修复
- 设置页「备份」点击无效果:SAF 目录授权失效(如重装)时原实现静默失败;现明确提示成功/失败,失败时自动弹出目录选择器引导重选。
### 变更
- 备份/恢复按钮更名:「备份到文件/网盘」→「备份」,「从文件恢复」→「恢复」;恢复统一走 ViewModel 并提示恢复条数。
- 每日详情时段明细排版:左侧固定单列大时段标识(纯文字,去掉色块底),右侧时间段改双列且单行不换行。

## [1.10.0] - 2026-09-10
### 新增
- 热力图每日详情:**时段明细双列展示**,每条时段最左标注大时段标识(凌晨/早上/上午/下午/晚上)。
- 通知栏空闲态:启动操作改为**右侧图标按钮**(自定义 RemoteViews,不再是文字 action)。

### 修复
- 通知栏状态栏小图标改用**应用图标同款火焰**(此前是系统闹钟图标,与应用图标不一致),四种通知态(idle/计时/完成/占位)统一。

### 变更
- 热力图时段分合规则改为**展示层按"相邻间隔 ≤ 3 分钟合并"**(废除旧的"暂停超过阈值即分段"作为可见规则;入库仍按暂停留痕,>3 分钟的间隔才显示为两段)。

## [1.9.3] - 2026-09-07
### 变更
- 设计系统一致性:统一 `Spacing`/`BarAnim` 动效 token。
- 报表趋势/时段比例条改为从 0 → 目标的弹性生长(尊重系统"关闭动画")。
- 设置页分区卡片化;配色色块改为等宽。

## [1.9.2] - 2026-09-07
### 修复
- 通知栏 RemoteViews 崩溃:`Space`/裸 `View` 不在 RemoteViews 允许类内,改为白名单内的 `Chronometer`(weight + `gravity=end`)——真机"点开始计时"不再闪退。
- 空态「去新建时钟」改为直达「时钟管理」(而非设置)。

## [1.9.1] - 2026-09-07
### 新增
- 常驻自定义通知:图标按钮(终止|开始/暂停|跳过)、倒计时与标题同排等宽、循环图标+计数;单渠道单 ID;RemoteViews 安全属性。
- 报表页「上一期/下一期」周期导航。
- 设置页数据导出(SAF→JSON)/导入(JSON→upsert)。

## [1.9.0] - 2026-09-07
### 新增
- 通知栏启动即常驻(空闲),计时开始切换计时态;单渠道单 ID。
- GitHub 式热力图(周日起始、Mon/Wed/Fri 标签、月份标签放含 1 日之列)。

## [1.8.7] - 2026-09-05
- 常驻通知;首页循环图标;按钮顺序 停止|暂停/恢复|跳过;中文应用名「余烬计时」;首装即建渠道;无"天"倒计时。

## [1.6.0] - 2026-09-04
- 平缓页面切换(纯交叉淡入);不足 1 分钟按误触处理(不累计/不存储);报表长期页更名「总报」。

## [1.5.0] - 2026-09-03
- 按钮动画闪帧修复;重构周/月报(健康风摘要、指标、时段分布);通知栏始终最多一条。

## [1.0.0] - 2026-09-04
- 完整应用:12 项刷新(图例/标题/键序、时钟管理页、正计时、报表、数据继承保障)。

## [0.5.0] - 2026-09-04
- 图标形变引擎(morph);统一动效/系统。

## [0.3.0 - 0.4.0]
- 计时引擎(倒计时/正计时/循环);热力图 v2;报表结构;动效 token。
